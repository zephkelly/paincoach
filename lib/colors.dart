import 'package:flutter/material.dart';

class AppColors {
  static const Color panelColour = Color.fromRGBO(28, 28, 30, 1);
  static const Color backgroundColour = Color.fromRGBO(20, 20, 20, 1);
  // Add other global colors here
}